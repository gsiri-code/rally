[project]
name = "ai-service"
version = "0.1.0"
description = "Workspace for travel planner services"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[tool.uv.workspace]
members = [
    "src/rag",
    "src/planner_service",
    "src/transcription_service",
    "src/web_agent_service",
    "src/transcription",
    "src/web_agent",
    "src/planner_agent",
    "src/orchestrator_agent",
    "src/api",
    "src/warehouse_db",
    "src/models",
]

[tool.uv.sources]
orchestrator-agent = { workspace = true }
