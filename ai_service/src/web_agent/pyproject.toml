[project]
name = "web-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "sirichartchai", email = "gs3802@nyu.edu" }
]
requires-python = ">=3.11"
dependencies = [
    "anthropic>=0.52.0",
    "orchestrator-agent",
    "pydantic>=2.11.7",
]

[project.scripts]
web-agent = "web_agent:main"

[tool.uv.sources]
orchestrator-agent = { workspace = true }

[build-system]
requires = ["uv_build>=0.8.17,<0.9.0"]
build-backend = "uv_build"
