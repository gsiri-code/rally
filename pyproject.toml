[project]
name = "rally"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "langchain>=1.2.10",
    "langchain-nvidia-ai-endpoints>=0.3.18",
    "python-dotenv>=1.0.1",
    "qdrant-client>=1.15.1",
]

[tool.setuptools]
py-modules = ["main", "embed_test"]
